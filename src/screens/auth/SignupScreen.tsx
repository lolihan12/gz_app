import React from 'react';
import { Button, requireNativeComponent, SafeAreaView, Text, View } from 'react-native';

const SignupScreen = () => {
	return (
		<SafeAreaView>
			<View>
				<Text>
					회원가입
				</Text>
			</View>
		</SafeAreaView>
	);
};

export default SignupScreen;